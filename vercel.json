{
    "version": 2,
    "buildCommand": "cd client && npm install && npm run build",
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/node",
            "config": {
                "includeFiles": [
                    "client/dist/**",
                    "client/public/**",
                    "routes/**",
                    "controllers/**",
                    "models/**",
                    "middleware/**",
                    "scripts/**",
                    "firebaseAdmin.js"
                ]
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server.js"
        }
    ]
}